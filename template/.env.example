# Copiar a .env.local y completar. NUNCA commitear .env.local.

# Postgres (Supabase o Neon). Se obtiene desde el panel del proveedor.
DATABASE_URL="postgresql://usuario:password@host:5432/basededatos?sslmode=require"

# Secreto de sesión. Generar con: npx auth secret
AUTH_SECRET=""

# Se completa en la clase 6, cuando agreguemos autenticación.
# AUTH_GOOGLE_ID=""
# AUTH_GOOGLE_SECRET=""

# Regla: ninguna variable con un secreto puede llamarse NEXT_PUBLIC_*,
# porque todo lo que lleva ese prefijo queda visible en el navegador.
